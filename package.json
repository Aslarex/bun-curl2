{
  "name": "bun-curl",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "DEBUG=* bun run --hot test/basic.ts",
    "dev:cache": "DEBUG=* bun run --hot test/with_cache.ts",
    "build": "rm -rf dist && tsc --project tsconfig.esm.json",
    "prettier": "prettier src/ --write"
  },
  "dependencies": {
    "prettier": "^3.5.2"
  },
  "optionalDependencies": {
    "redis": "^4.7.0"
  }
}